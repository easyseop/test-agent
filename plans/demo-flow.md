# 테스트 계획 — 주문 관리 대시보드

> 이 파일은 화면을 훑어 자동 생성된 **질문 목록**이다.
> **기대값 칸이 비어 있으면 그 항목은 테스트로 만들지 않는다.**
> 빈칸은 설명서·기획서·업무 규칙에서 채운다. 화면을 보고 채우지 않는다 —
> 지금 화면이 틀렸을 수도 있기 때문이다.
>
> 생성: `/plan-tests` · 대상 `http://127.0.0.1:5057`

## 화면: / (주문 관리 대시보드)

**관찰한 것** (사실만)

- 표 `#orders-table` — **123행**, 열: 주문번호 | 고객 | 상태 | 카테고리 | 금액 | 주문일
- 셀렉트 `#status` — 선택지: (빈값) · pending · shipped · delivered · cancelled
- 셀렉트 `#category` — 선택지: (빈값) · 전자기기 · 의류 · 식품
- 입력 `#q`(검색) · `#date-from` · `#date-to`
- 버튼 `#apply`(조회) · `#refresh`(새로고침) · `#summary-btn`(요약 보기)
- 정답원 후보: `GET /api/orders` → `rows_path: orders`, 123개 항목,
  필드 `id · customer · status · category · amount · created_at`

**확인 항목**

- [x] **상태 필터**
  - 하는 일: `#status`를 고르고 `#apply`를 누른다
  - ✅ 무엇이 나와야 하나: **그 상태인 주문만.** 배송완료된 주문은 이미 발송을
       거쳤어도 지금 상태가 아니므로 나오면 안 된다
  - ✅ 근거: `docs/demo-app-spec.md` §2-1
       *"담당자가 상태 하나를 고르면 그 상태인 주문만 목록에 나온다"*

- [x] **카테고리 필터**
  - 하는 일: `#category`를 고르고 `#apply`를 누른다
  - ✅ 무엇이 나와야 하나: **그 카테고리 주문만**
  - ✅ 근거: `docs/demo-app-spec.md` §2-4

- [x] **날짜 범위 필터**
  - 하는 일: `#date-from`·`#date-to`를 채우고 `#apply`를 누른다
  - ✅ 무엇이 나와야 하나: 그 기간에 들어온 주문만
  - ✅ **시작일·종료일은 둘 다 포함.** 6/1~6/30으로 조회하면 6월 30일에 들어온
       주문도 반드시 포함된다
  - ✅ 근거: `docs/demo-app-spec.md` §2-2
       *"시작일과 종료일은 둘 다 포함한다"* — 빠지면 매출 집계가 어긋난다

- [x] **고객 검색**
  - 하는 일: `#q`에 글자를 넣고 `#apply`를 누른다
  - ✅ 무엇이 나와야 하나: **부분일치.** "김"으로 검색하면 김철수·김영희가 모두 나온다
  - ✅ 근거: `docs/demo-app-spec.md` §2-3

- [x] **표와 정답원 일치**
  - 하는 일: `#orders-table`을 읽어 `GET /api/orders`와 행 단위로 대조한다
  - ✅ 어느 열을 대조해야 하나: 주문번호 · 고객 · 상태 · 금액
  - ✅ 순서도 같아야 하나: **아니오.** 설명서에 정렬 규칙이 없으므로 순서는 안 본다

## 확인하지 못한 것

- `#danger-delete` (전체 삭제) — **되돌릴 수 없어 보여 누르지 않았다.**
  이 버튼의 동작은 이 계획에서 다루지 않는다.
- `/new` (주문 등록) — 저장이 일어나는 화면이라 이번 계획에서 제외했다.
  등록을 검증하려면 시드 DB와 `--allow-write-checks` 승인이 필요하다.
- `/about` — 표·입력이 없어 검증할 것이 없다.

## 빈칸 수: 0개 — 채움 완료 (2026-08-13, 근거: `docs/demo-app-spec.md`)

---

## 실행 결과 (2026-08-13)

이 계획으로 `configs/demo-flow.yaml`을 만들어 두 번 돌렸다.

| 회차 | 결과 | 종료코드 |
|---|---|---|
| 1차 | 통과 3 · **판정 불가 2** | 2 |
| 2차 | 통과 5 · 대조 287행 | 0 |

**1차에서 도구 결함이 하나 드러났다.** 한글이 든 정답원 주소
(`?category=전자기기`, `?q=김`)를 인코딩하지 않고 보내 조회가 실패했다.

주목할 점은 그때 도구가 **'실패'가 아니라 '판정 불가'로 보고했다**는 것이다.
화면이 맞는지 틀린지 알 수 없는 상태였으므로 그게 맞는 답이다. 제품 탓으로
돌렸다면 멀쩡한 데모앱 코드를 뒤졌을 것이다.

고친 뒤 2차에서 5개 전부 통과했고, 리포트에 `전회차 대비: 복구 2`가 찍혔다.
